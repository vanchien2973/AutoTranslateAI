# Keep shell scripts and Dockerfiles LF so they run inside Linux containers
# even when the repo is checked out on Windows.
*.sh            text eol=lf
Dockerfile*     text eol=lf
*.yml           text eol=lf
*.yaml          text eol=lf

# Let git handle normal text normalization elsewhere.
* text=auto
